import 'package:flutter/material.dart';

class AllChannel extends StatelessWidget {
  const AllChannel({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('All Channel'),
      )
    );
  }
}